Run the full quality gate pipeline: typecheck, test, and build.

Report each step's result (pass/fail) and stop on the first failure.

```bash
pnpm gates
```

If any step fails, diagnose the root cause and fix it before re-running.
